# Opportunistically multiplex connections; use `ssh -S none` to avoid
# this, e.g., for transferring large data volumes.
Host *
  ServerAliveInterval 30
  ForwardX11Timeout 0
  ControlMaster auto
  ControlPath ~/.ssh/%u.ssh.%r@%h-%p
  ControlPersist 300s

# github-keygen manages ~/.ssh/github-keygen; run it to set up GitHub SSH access:
#   git clone https://github.com/dolmen/github-keygen.git
#   cd github-keygen
#   ./github-keygen <my-github-username>
#   cd ..
#   rm -Rf github-keygen
Include github-keygen
